# 轻便侠218 多图测试口播修正 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 生成一份保持原三句台词与现有运镜风格、同时禁止开场重复并保持陪护者入镜的 MiniMax-H3 多图测试提示词候选。

**Architecture:** 以已通过的 V03 提示词为基线，只替换口播时间轴与运镜边界。执行前分别加载 `audio`、`video` 节点正式规则，生成后使用确定性文本检查验证台词次数、时间段、禁止项和入镜要求，不调用付费接口。

**Tech Stack:** UTF-8 文本文件、PowerShell、Python `workflow_cli.py`、ripgrep。

## Global Constraints

- 三句既定台词文本不得改变。
- 继续使用模型原生双角色对白，不增加独立参考音频。
- 开场问题及其同义表达全片只出现一次。
- 运镜保持连续自然，允许轻微平稳推近。
- 老人、陪护者和完整轮椅全程入镜。
- 本计划只生成提示词候选，不联网、不付费提交。

---

### Task 1: 生成并校验修正版提示词候选

**Files:**
- Read: `Y:\0-AI训练\光合视频\轻便侠218 产品图片\生成视频\20260831_批次001\V001_防滑安全_待生成\_工作文件\生成过程\视频提示词_V03候选.txt`
- Create: `Y:\0-AI训练\光合视频\轻便侠218 产品图片\生成视频\20260831_批次001\V001_防滑安全_待生成\_工作文件\生成过程\视频提示词_多图测试修正版候选.txt`
- Create: `Y:\0-AI训练\光合视频\轻便侠218 产品图片\生成视频\20260831_批次001\V001_防滑安全_待生成\_工作文件\验收记录\视频提示词_多图测试修正版检查.md`

**Interfaces:**
- Consumes: V03 场景、产品、人物、三句原始台词，以及 `生效规则_audio.json`、`生效规则_video.json`。
- Produces: 一份供用户逐字审核的 UTF-8 提示词候选和一份确定性检查记录。

- [ ] **Step 1: 加载节点规则**

运行：

```powershell
python C:\Users\Administrator\.agents\skills\product-video-pipeline\scripts\workflow_cli.py prepare-node-rules --knowledge-dir C:\Users\Administrator\.agents\skills\product-video-pipeline\data --batch "Y:\0-AI训练\光合视频\轻便侠218 产品图片\生成视频\20260831_批次001" --video-id V001 --node audio --model MiniMax-H3多图多音频15秒 --channel AutoDL.Art
python C:\Users\Administrator\.agents\skills\product-video-pipeline\scripts\workflow_cli.py prepare-node-rules --knowledge-dir C:\Users\Administrator\.agents\skills\product-video-pipeline\data --batch "Y:\0-AI训练\光合视频\轻便侠218 产品图片\生成视频\20260831_批次001" --video-id V001 --node video --model MiniMax-H3多图多音频15秒 --channel AutoDL.Art
```

预期：生成并读取 `生效规则_audio.json` 与 `生效规则_video.json`，把每条 `required_action` 注入候选提示词。

- [ ] **Step 2: 写入候选提示词**

候选必须包含以下逐字台词和唯一时间段：

```text
0–3秒，P2儿子仅说一次：“爸，菜市场地湿，会打滑吗？”
3–8秒，P1老人仅说一次：“抓地稳，直线走很踏实。”
8–15秒，P1老人仅说一次：“用了爱优护电动轮椅后更省心，可以了解后下单。”
```

并包含：开场问题结束后 P2 保持安静；禁止重复、复述、回声、同义改写、新增台词和角色代说；允许轻微连续推近，但 P1、P2 与完整轮椅始终位于安全画框内。

- [ ] **Step 3: 执行确定性文本检查**

运行：

```powershell
$prompt = Get-Content -LiteralPath "Y:\0-AI训练\光合视频\轻便侠218 产品图片\生成视频\20260831_批次001\V001_防滑安全_待生成\_工作文件\生成过程\视频提示词_多图测试修正版候选.txt" -Raw
@(
  $prompt.Contains('0至3秒'),
  $prompt.Contains('3至8秒'),
  $prompt.Contains('8至15秒'),
  ([regex]::Matches($prompt, '爸，菜市场地湿，会打滑吗？')).Count -eq 1,
  ([regex]::Matches($prompt, '抓地稳，直线走很踏实。')).Count -eq 1,
  ([regex]::Matches($prompt, '用了爱优护电动轮椅后更省心，可以了解后下单。')).Count -eq 1,
  $prompt.Contains('禁止重复'),
  $prompt.Contains('陪护者不得出画')
) -notcontains $false
```

预期：输出 `True`。将检查项目与结果写入验收记录，但不把候选晋升为正式提示词。

- [ ] **Step 4: 交付用户逐字审核**

向用户展示完整候选提示词，并要求明确回复“通过修正版提示词”或“不通过 + 修改要求”。未通过前不得创建付费提交请求。

- [ ] **Step 5: 提交本任务文档变更**

```powershell
git add docs/superpowers/plans/2026-08-31-轻便侠218多图测试口播修正.md
git commit -m "docs: 添加轻便侠218口播修正执行计划"
```

预期：仅提交该计划文件，不包含工作区其他已有未跟踪文件。
