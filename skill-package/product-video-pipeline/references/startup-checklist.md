# 任务启动确认单

## API 前置规则

安装后首次使用技能时，第一步必须询问 AutoDL API 是否已接入。API 未接入时，只提供安全配置指导并暂停，不得询问产品参数、扫描产品图片、创建批次或调用付费接口。不得要求用户把完整 API Key 粘贴到聊天中。

API 接入后，先完成不联网、不扣费的 dry-run。只有 API 配置可用，才展示下方任务启动确认单。API 就绪不代表已获准付费，正式提交仍需单独确认价格和预算。

正式执行前，把以下内容一次性展示给用户并获得确认。未确认时可以扫描、规划和 dry-run，不得调用付费视频接口。

```text
产品文件夹路径：
识别的产品名称：
第一层产品图片：
本次卖点清单：
计划生成视频总数：
各卖点分配数量：

运行模式：学习确认 / 自动生产
文本生成能力：当前 Agent 原生 / API 名称、base_url、模型、密钥环境变量
生图能力：当前 Agent 原生 / API 名称、base_url、模型、密钥环境变量
封面图参考文件夹：
视频模型：AutoDL.Art MiniMax-H3
AutoDL 提交端点与查询端点：
AutoDL 鉴权格式：Bearer / raw token
视频时长：15 秒
画幅：9:16
分辨率：768P / 2K（默认建议 768P，必须明确询问是否改 2K）
人物配音：MiniMax-H3 根据人物清单匹配自然音色
声音：对白 + 轻微环境声，无 BGM
AIGC 画面水印：关闭

当前实时单价及会员状态：
价格查询时间和来源：
单条预计费用：
批次基础费用：
包含一次重跑的最高费用：
本批次最高预算：
并发：3
分镜图最大尝试次数：3
视频最大重跑次数：1
输出批次文件夹：
采用的产品配置和正式经验版本：
```

## 运行中不再补问

确认后，不要在中间节点才询问文本模型、生图模型、配音、分辨率、普通重试次数或预算。以下情况可以暂停：

- 学习模式预先约定的策划与分镜审核；
- API 整体鉴权失效、余额不足、预算即将超限或必要配置失效；
- 批次结束后的人工视频验收；
- 学习模式重跑前的统一修改方案和费用确认。

实时价格只在任务开始时记录。技能包不把历史截图价格当作永久报价。
