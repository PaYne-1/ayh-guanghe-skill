# product-video-pipeline 1.8.5

- 本批次最高总预算仅统计视频费用，图片和文本不占用、不参与预算拦截；服务商可能另行收费。
- 生图单价可选，不询问；配置读取、init、自动激活、图片动作、断点重载和交付均支持未知单价，记录 null 而非免费。
- 视频价格和预算校验、V02 单独授权、动作去重和密钥保护保持有效。
- 更新完整技能包并重启对话。兼容旧预算清单的摘要校验，不改写已封存授权、已有产物或任务 ID，禁止盲目重提交。
- 本版本测试不调用付费服务，不包含真实密钥。
