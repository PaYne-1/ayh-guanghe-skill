# 学习模式自动复盘与规避实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让学习模式中的任务相关用户问题自动形成候选复盘，在 V02 对应验证通过后自动升级正式规则，并在后续学习模式和自动模式启动及节点执行前自动加载和应用。

**Architecture:** 在现有 `workflow_cli.py` 中增加知识规则存取、验证升级、匹配和命中记录，继续复用既有原子 JSON 写入与文件锁。候选经验与正式规则分别落入知识目录；批次初始化写入命中摘要，每个节点通过独立命令生成生效规则清单，Agent 必须读取后才能继续。

**Tech Stack:** Python 3、标准库 JSON/Path/hashlib/datetime、pytest、现有 `workflow_cli.py` 原子写入与跨进程锁。

## Global Constraints

- 最终视频仍由用户人工验收。
- 每条视频仍只允许 V02 一次付费重跑。
- 未验证或验证结果不确定的方案不能成为正式规则。
- 学习模式验证通过后自动升级，不再额外询问用户。
- 自动模式只读取正式规则，不读取候选经验。
- 不保存标题库、脚本库或分镜库。
- 产品专属规则高于全局规则，限定更精确的规则优先。
- 知识写入必须使用临时文件、原子替换和跨进程锁。
- 不修改或覆盖工作区中的无关用户文件。

---

### Task 1: 候选经验结构化记录

**Files:**
- Modify: `skill-package/product-video-pipeline/scripts/workflow_cli.py`
- Test: `tests/test_portable_skill_package.py`

**Interfaces:**
- Consumes: `knowledge_dir: Path`、批次启动确认单、视频 ID、用户原话及 Agent 自动复盘字段。
- Produces: `capture_learning_issue`，返回候选经验 JSON 路径 `Path`。

- [ ] **Step 1: 写候选经验失败测试**

```python
def test_capture_learning_issue_preserves_feedback_and_structured_review(tmp_path):
    runtime = load_script("workflow_cli.py")
    batch = tmp_path / "20260829_批次001"
    batch.mkdir()
    (batch / "启动确认单.json").write_text(
        json.dumps({"run_mode": "learning", "product_name": "轻便侠", "product_dir": str(tmp_path / "轻便侠")}),
        encoding="utf-8",
    )
    candidate = tmp_path / "candidate.png"
    candidate.write_bytes(b"candidate")

    path = runtime.capture_learning_issue(
        knowledge_dir=tmp_path / "knowledge",
        batch_dir=batch,
        video_id="V001",
        node="storyboard",
        user_feedback="轮椅扶手结构画错了",
        symptom="右侧扶手缺失",
        root_cause="参考图约束没有写入保持项",
        solution="在提示词保持项中锁定左右扶手",
        prevention_rule="提交生图前检查保持项包含左右扶手",
        validation_method="V02 左右扶手均存在且用户通过",
        validation_expected="左右扶手结构与主参考图一致",
        evidence_paths=(candidate,),
        model="Codex ImageGen",
        channel="本机 Codex 界面",
    )

    value = json.loads(path.read_text(encoding="utf-8"))
    assert value["status"] == "candidate"
    assert value["user_feedback"] == "轮椅扶手结构画错了"
    assert value["validation"]["result"] == "pending"
    assert value["evidence"][0]["sha256"] == runtime._sha256_file(candidate)
```

- [ ] **Step 2: 运行测试并确认因接口不存在而失败**

Run: `pytest -q tests/test_portable_skill_package.py::test_capture_learning_issue_preserves_feedback_and_structured_review`

Expected: FAIL，提示 `workflow_cli` 没有 `capture_learning_issue`。

- [ ] **Step 3: 实现候选经验写入**

在 `workflow_cli.py` 增加以下接口和校验：

```python
LEARNING_NODES = {"content", "storyboard", "cover", "audio", "video", "api", "polling", "download", "review"}


def capture_learning_issue(
    *,
    knowledge_dir: Path,
    batch_dir: Path,
    video_id: str,
    node: str,
    user_feedback: str,
    symptom: str,
    root_cause: str,
    solution: str,
    prevention_rule: str,
    validation_method: str,
    validation_expected: str,
    evidence_paths: Sequence[Path] = (),
    model: Optional[str] = None,
    channel: Optional[str] = None,
    now: Optional[datetime] = None,
) -> Path:
    if node not in LEARNING_NODES:
        raise ValueError(f"未知学习节点：{node}")
    required = {
        "用户反馈": user_feedback,
        "失败表现": symptom,
        "根因": root_cause,
        "解决方案": solution,
        "预防规则": prevention_rule,
        "验证方法": validation_method,
        "预期结果": validation_expected,
    }
    empty = [name for name, value in required.items() if not str(value).strip()]
    if empty:
        raise ValueError("自动复盘缺少：" + "、".join(empty))
    confirmation = read_json(batch_dir / "启动确认单.json")
    if confirmation.get("run_mode") != "learning":
        raise ValueError("只有学习模式可以记录新的候选经验")
    timestamp = (now or datetime.now().astimezone()).isoformat()
    fingerprint_source = "|".join(
        (str(confirmation.get("product_name", "")), node, video_id, user_feedback.strip(), symptom.strip())
    )
    issue_id = "issue_" + hashlib.sha256(fingerprint_source.encode("utf-8")).hexdigest()[:16]
    evidence = []
    for source in evidence_paths:
        resolved = source.resolve()
        evidence.append({"path": str(resolved), "sha256": _sha256_file(resolved)})
    product_name = str(confirmation.get("product_name", ""))
    value = {
        "issue_id": issue_id,
        "status": "candidate",
        "scope": {
            "product_id": _product_id(product_name) if product_name else None,
            "node": node,
            "model": model,
            "channel": channel,
        },
        "user_feedback": user_feedback.strip(),
        "symptom": symptom.strip(),
        "root_cause": root_cause.strip(),
        "solution": solution.strip(),
        "prevention_rule": prevention_rule.strip(),
        "validation": {"method": validation_method.strip(), "expected": validation_expected.strip(), "result": "pending"},
        "evidence": evidence,
        "source_batch": str(batch_dir.resolve()),
        "source_video_id": video_id,
        "created_at": timestamp,
        "updated_at": timestamp,
    }
    target = knowledge_dir.resolve() / "候选经验" / f"{issue_id}.json"
    existing = read_json(target, None)
    if existing is not None:
        existing.setdefault("duplicate_events", []).append({"at": timestamp, "evidence": evidence})
        existing["updated_at"] = timestamp
        atomic_write_json(target, existing)
    else:
        atomic_write_json(target, value)
    return target
```

- [ ] **Step 4: 运行候选经验测试和现有验收测试**

Run: `pytest -q tests/test_portable_skill_package.py::test_capture_learning_issue_preserves_feedback_and_structured_review tests/test_portable_skill_package.py::test_record_review_writes_item_evidence_to_work_dir`

Expected: 2 passed。

- [ ] **Step 5: 提交本任务**

```powershell
git add -- skill-package/product-video-pipeline/scripts/workflow_cli.py tests/test_portable_skill_package.py
git commit -m "feat: capture structured learning issues"
```

### Task 2: V02 验证与正式规则自动升级

**Files:**
- Modify: `skill-package/product-video-pipeline/scripts/workflow_cli.py`
- Test: `tests/test_portable_skill_package.py`

**Interfaces:**
- Consumes: `issue_id`、验证结果、已通过候选文件、批次与视频 ID。
- Produces: `validate_learning_issue`；通过时返回正式规则路径 `Path`，失败或不确定时返回 `None`。

- [ ] **Step 1: 写自动升级和拒绝升级失败测试**

```python
def test_validate_learning_issue_promotes_only_verified_v02(tmp_path):
    runtime = load_script("workflow_cli.py")
    knowledge = tmp_path / "knowledge"
    batch = tmp_path / "batch"
    batch.mkdir()
    (batch / "启动确认单.json").write_text(json.dumps({"run_mode": "learning"}), encoding="utf-8")
    item = batch / "V001_卖点_待生成"
    runtime.ensure_work_dirs(item)
    runtime.atomic_write_json(runtime.work_path(item, "任务状态", "任务信息.json"), {"video_id": "V001", "retry_count": 1})
    approved = item / "_工作文件" / "生成过程" / "视频候选.mp4"
    approved.write_bytes(b"verified-v02")
    issue = knowledge / "候选经验" / "issue_a.json"
    runtime.atomic_write_json(issue, {
        "issue_id": "issue_a", "status": "candidate",
        "scope": {"product_id": "p1", "node": "video", "model": "H3", "channel": "AutoDL"},
        "user_feedback": "尾句被截断", "symptom": "结尾听不完整", "root_cause": "口播超时",
        "solution": "缩短口播", "prevention_rule": "提交前检查口播在14秒内结束",
        "validation": {"method": "V02转写完整", "expected": "尾句完整", "result": "pending"},
        "source_batch": str(batch.resolve()), "source_video_id": "V001", "created_at": "2026-08-29T00:00:00+08:00"
    })

    rule_path = runtime.validate_learning_issue(
        knowledge_dir=knowledge, batch_dir=batch, video_id="V001", issue_id="issue_a",
        result="passed", verified_candidate=approved,
    )

    rule = json.loads(rule_path.read_text(encoding="utf-8"))
    assert rule["status"] == "active"
    assert rule["required_action"] == "提交前检查口播在14秒内结束"
    assert rule["verified_candidate_sha256"] == runtime._sha256_file(approved)
    assert json.loads(issue.read_text(encoding="utf-8"))["status"] == "promoted"


@pytest.mark.parametrize("result", ["failed", "inconclusive"])
def test_validate_learning_issue_does_not_promote_unverified_results(tmp_path, result):
    runtime = load_script("workflow_cli.py")
    knowledge = tmp_path / "knowledge"
    batch = tmp_path / "batch"
    batch.mkdir()
    (batch / "启动确认单.json").write_text(json.dumps({"run_mode": "learning"}), encoding="utf-8")
    item = batch / "V001_卖点_待生成"
    runtime.ensure_work_dirs(item)
    runtime.atomic_write_json(
        runtime.work_path(item, "任务状态", "任务信息.json"),
        {"video_id": "V001", "retry_count": 1},
    )
    issue = knowledge / "候选经验" / "issue_a.json"
    runtime.atomic_write_json(issue, {
        "issue_id": "issue_a", "status": "candidate",
        "scope": {"product_id": "p1", "node": "video", "model": "H3", "channel": "AutoDL"},
        "user_feedback": "尾句被截断", "symptom": "结尾听不完整", "root_cause": "口播超时",
        "solution": "缩短口播", "prevention_rule": "提交前检查口播在14秒内结束",
        "validation": {"method": "V02转写完整", "expected": "尾句完整", "result": "pending"},
        "source_batch": str(batch.resolve()), "source_video_id": "V001",
        "created_at": "2026-08-29T00:00:00+08:00",
    })

    promoted = runtime.validate_learning_issue(
        knowledge_dir=knowledge, batch_dir=batch, video_id="V001", issue_id="issue_a",
        result=result, verified_candidate=None,
    )

    assert promoted is None
    assert not list((knowledge / "正式规则").glob("*.json"))
    value = json.loads(issue.read_text(encoding="utf-8"))
    assert value["status"] == result
    assert value["validation"]["result"] == result
```

- [ ] **Step 2: 运行测试并确认因验证接口不存在而失败**

Run: `pytest -q tests/test_portable_skill_package.py -k "validate_learning_issue"`

Expected: FAIL，提示缺少 `validate_learning_issue`。

- [ ] **Step 3: 实现验证升级、版本替代和审计**

实现以下固定行为：

```python
def validate_learning_issue(
    *, knowledge_dir: Path, batch_dir: Path, video_id: str, issue_id: str,
    result: str, verified_candidate: Optional[Path], now: Optional[datetime] = None,
) -> Optional[Path]:
    if result not in {"passed", "failed", "inconclusive"}:
        raise ValueError("学习验证结果无效")
    issue_path = knowledge_dir.resolve() / "候选经验" / f"{issue_id}.json"
    issue = read_json(issue_path)
    item = _find_item(batch_dir, video_id)
    task = read_json(work_path(item, "任务状态", "任务信息.json"))
    if int(task.get("retry_count", 0)) != 1:
        raise ValueError("只有 V02 唯一一次重跑可以验证候选经验")
    timestamp = (now or datetime.now().astimezone()).isoformat()
    issue["validation"]["result"] = result
    issue["validation"]["verified_at"] = timestamp
    issue["updated_at"] = timestamp
    if result != "passed":
        issue["status"] = result
        atomic_write_json(issue_path, issue)
        return None
    if verified_candidate is None or not verified_candidate.is_file():
        raise ValueError("验证通过必须绑定实际候选文件")
    digest = _sha256_file(verified_candidate)
    scope = issue["scope"]
    rule_key = "|".join(str(scope.get(key) or "*") for key in ("product_id", "node", "model", "channel"))
    rule_id = "rule_" + hashlib.sha256((rule_key + "|" + issue["prevention_rule"]).encode("utf-8")).hexdigest()[:16]
    rules_dir = knowledge_dir.resolve() / "正式规则"
    target = rules_dir / f"{rule_id}.json"
    previous = read_json(target, None)
    version = int(previous.get("version", 0)) + 1 if previous else 1
    rule = {
        "rule_id": rule_id, "version": version, "status": "active", "scope": scope,
        "trigger": issue["symptom"], "required_action": issue["prevention_rule"],
        "validation_check": issue["validation"]["expected"], "source_issue_id": issue_id,
        "source_batch": str(batch_dir.resolve()), "source_video_id": video_id,
        "verified_candidate_sha256": digest, "verified_at": timestamp,
        "hit_count": int(previous.get("hit_count", 0)) if previous else 0,
        "last_hit_at": previous.get("last_hit_at") if previous else None,
    }
    atomic_write_json(target, rule)
    issue["status"] = "promoted"
    issue["promoted_rule_id"] = rule_id
    atomic_write_json(issue_path, issue)
    return target
```

若出现相同范围但动作不同的 active 规则，新增规则前把旧规则状态改为 `superseded`，写入 `superseded_by`，并为新规则使用递增版本。此逻辑必须有单独测试。

- [ ] **Step 4: 运行验证升级测试**

Run: `pytest -q tests/test_portable_skill_package.py -k "validate_learning_issue or superseded"`

Expected: 全部通过。

- [ ] **Step 5: 提交本任务**

```powershell
git add -- skill-package/product-video-pipeline/scripts/workflow_cli.py tests/test_portable_skill_package.py
git commit -m "feat: promote verified learning rules"
```

### Task 3: 下次任务匹配、节点注入与命中记录

**Files:**
- Modify: `skill-package/product-video-pipeline/scripts/workflow_cli.py`
- Test: `tests/test_portable_skill_package.py`

**Interfaces:**
- Consumes: 当前产品、节点、模型、渠道和 `正式规则/*.json`。
- Produces: `load_matching_rules` 返回 `List[dict]`、批次 `命中正式规则.json`、单条 `_工作文件/任务状态/生效规则_<node>.json`。

- [ ] **Step 1: 写规则优先级和批次初始化失败测试**

```python
def test_load_matching_rules_prefers_product_and_precise_scope(tmp_path):
    runtime = load_script("workflow_cli.py")
    rules = tmp_path / "正式规则"
    runtime.atomic_write_json(rules / "global.json", {
        "rule_id": "global", "version": 1, "status": "active",
        "scope": {"product_id": None, "node": "video", "model": None, "channel": None},
        "required_action": "全局动作", "verified_at": "2026-08-28T00:00:00+08:00", "hit_count": 0,
    })
    runtime.atomic_write_json(rules / "product.json", {
        "rule_id": "product", "version": 1, "status": "active",
        "scope": {"product_id": "p1", "node": "video", "model": "H3", "channel": "AutoDL"},
        "required_action": "产品动作", "verified_at": "2026-08-29T00:00:00+08:00", "hit_count": 0,
    })

    matched = runtime.load_matching_rules(tmp_path, product_id="p1", node="video", model="H3", channel="AutoDL")

    assert [rule["rule_id"] for rule in matched] == ["product", "global"]
```

另写初始化测试，断言 `启动确认单.json` 包含 `matched_learning_rules` 和正式规则库路径。

- [ ] **Step 2: 运行测试并确认失败**

Run: `pytest -q tests/test_portable_skill_package.py -k "matching_rules or matched_learning_rules"`

Expected: FAIL，提示接口或确认单字段不存在。

- [ ] **Step 3: 实现匹配与节点清单**

```python
def load_matching_rules(
    knowledge_dir: Path, *, product_id: Optional[str], node: Optional[str],
    model: Optional[str] = None, channel: Optional[str] = None,
) -> List[dict]:
    rules_dir = knowledge_dir.resolve() / "正式规则"
    matched = []
    for path in sorted(rules_dir.glob("*.json")) if rules_dir.exists() else ():
        rule = read_json(path)
        if rule.get("status") != "active":
            continue
        scope = rule.get("scope", {})
        checks = (("product_id", product_id), ("node", node), ("model", model), ("channel", channel))
        if any(value is not None and scope.get(key) not in (None, value) for key, value in checks):
            continue
        specificity = sum(scope.get(key) is not None for key, _ in checks)
        rule["_specificity"] = specificity
        matched.append(rule)
    matched.sort(
        key=lambda value: (value["_specificity"], value.get("verified_at", ""), value["rule_id"]),
        reverse=True,
    )
    for rule in matched:
        rule.pop("_specificity", None)
    return matched
```

给 `initialize_batch` 增加：

```python
matched_rules = load_matching_rules(
    knowledge_dir, product_id=_product_id(product_name), node=None, model=None, channel=None
)
confirmation["formal_rules_library"] = str((knowledge_dir.resolve() / "正式规则"))
confirmation["matched_learning_rules"] = [
    {"rule_id": r["rule_id"], "node": r["scope"].get("node"), "required_action": r["required_action"]}
    for r in matched_rules
]
```

增加 `prepare_node_rules` 并返回输出路径 `Path`，按节点、模型、渠道重新筛选，写入 `_工作文件/任务状态/生效规则_<node>.json`；写入后逐条更新正式规则的 `hit_count` 和 `last_hit_at`。更新必须加锁，避免并发丢计数。

- [ ] **Step 4: 运行规则匹配、初始化和并发命中测试**

Run: `pytest -q tests/test_portable_skill_package.py -k "matching_rules or matched_learning_rules or rule_hit"`

Expected: 全部通过；并发命中次数等于调用次数。

- [ ] **Step 5: 提交本任务**

```powershell
git add -- skill-package/product-video-pipeline/scripts/workflow_cli.py tests/test_portable_skill_package.py
git commit -m "feat: load and apply verified learning rules"
```

### Task 4: CLI 命令与验收回写连接

**Files:**
- Modify: `skill-package/product-video-pipeline/scripts/workflow_cli.py`
- Modify: `skill-package/product-video-pipeline/scripts/self_test.py`
- Test: `tests/test_portable_skill_package.py`

**Interfaces:**
- Produces CLI commands: `record-issue`、`validate-learning`、`prepare-node-rules`。

- [ ] **Step 1: 写三个 CLI 失败测试**

测试必须使用 `subprocess.run` 调用真实脚本：

```python
def test_learning_cli_commands_are_exposed():
    result = subprocess.run([sys.executable, str(SKILL_ROOT / "scripts" / "workflow_cli.py"), "--help"], capture_output=True, text=True)
    assert result.returncode == 0
    assert "record-issue" in result.stdout
    assert "validate-learning" in result.stdout
    assert "prepare-node-rules" in result.stdout
```

再分别创建最小临时批次，验证命令真实写入候选、正式规则和节点生效清单。

- [ ] **Step 2: 运行 CLI 测试并确认失败**

Run: `pytest -q tests/test_portable_skill_package.py -k "learning_cli"`

Expected: FAIL，帮助输出中没有新命令。

- [ ] **Step 3: 接入 argparse**

参数契约：

```text
record-issue --knowledge-dir --batch --video-id --node --feedback --symptom --root-cause --solution --prevention-rule --validation-method --validation-expected [--evidence PATH，可重复] [--model] [--channel]
validate-learning --knowledge-dir --batch --video-id --issue-id --result passed|failed|inconclusive [--candidate]
prepare-node-rules --knowledge-dir --batch --video-id --node [--model] [--channel]
```

`validate-learning --result passed` 缺少 `--candidate` 时必须报错；其他结果不要求候选文件。每个成功命令输出生成文件的绝对路径，便于其他智能体接续。

- [ ] **Step 4: 扩展 self_test 并运行**

`self_test.py` 必须检查三个命令出现在帮助中，并在临时目录完成一次候选记录、失败验证和规则匹配空结果。

Run: `python skill-package/product-video-pipeline/scripts/self_test.py`

Expected: exit code 0，输出自检通过。

- [ ] **Step 5: 提交本任务**

```powershell
git add -- skill-package/product-video-pipeline/scripts/workflow_cli.py skill-package/product-video-pipeline/scripts/self_test.py tests/test_portable_skill_package.py
git commit -m "feat: expose automatic learning workflow commands"
```

### Task 5: 技能规则、启动确认单与可移植交付文件

**Files:**
- Modify: `skill-package/product-video-pipeline/SKILL.md`
- Modify: `skill-package/product-video-pipeline/references/startup-checklist.md`
- Modify: `skill-package/product-video-pipeline/references/workflow.md`
- Modify: `skill-package/product-video-pipeline/references/review-learning.md`
- Create: `skill-package/product-video-pipeline/references/automatic-learning-rules.md`
- Preserve/Create: `skill-package/product-video-pipeline/references/image-generation-routing.md`
- Modify: `skill-package/product-video-pipeline/VERSION`
- Test: `tests/test_portable_skill_package.py`

**Interfaces:**
- Produces: 可单独发送给其他智能体的 `automatic-learning-rules.md`，以及从 `SKILL.md` 可发现的强制路由。

- [ ] **Step 1: 写文档契约失败测试**

```python
def test_automatic_learning_reference_is_complete_and_routed():
    skill = (SKILL_ROOT / "SKILL.md").read_text(encoding="utf-8")
    learning = (SKILL_ROOT / "references" / "automatic-learning-rules.md").read_text(encoding="utf-8")
    assert "references/automatic-learning-rules.md" in skill
    assert "用户反馈 → 自动复盘 → V02验证 → 自动升级正式规则 → 下次任务强制加载并执行" in learning
    assert "record-issue" in learning
    assert "validate-learning" in learning
    assert "prepare-node-rules" in learning
    assert "failed" in learning and "inconclusive" in learning
    assert "自动模式只读取正式规则" in learning
```

把 `references/automatic-learning-rules.md` 和现有 `references/image-generation-routing.md` 都加入可移植资源集合测试。

- [ ] **Step 2: 运行文档测试并确认失败**

Run: `pytest -q tests/test_portable_skill_package.py -k "automatic_learning_reference or image_generation_routing"`

Expected: 自动学习文件缺失测试失败；生图路由测试应通过。

- [ ] **Step 3: 写完整自动学习规则文件并接入入口**

`automatic-learning-rules.md` 必须完整覆盖设计文档的：适用范围、反馈捕获、结构化复盘、问题归类、V02 验证、自动升级、正式规则结构、下次任务加载、节点执行前注入、冲突处理、命中记录、安全写入、失败恢复和三条 CLI 命令示例。

在 `SKILL.md` 的 Required routing 增加：学习模式收到任务相关用户问题时立即读取自动学习规则；每次初始化学习或自动模式时读取正式规则摘要；每个节点开始前运行 `prepare-node-rules`。

在 `startup-checklist.md` 增加：正式规则库路径、正式规则版本摘要、本次命中规则 ID、排除规则及原因。

把 `review-learning.md` 原有“重跑通过后仍需用户确认升级”改为“学习模式中对应验证通过后自动升级，不再单独确认”；保留未经验证候选不能污染正式库。

- [ ] **Step 4: 版本升级为 1.4.0 并运行文档测试**

将 `VERSION` 和测试中的固定版本断言统一改为 `1.4.0`。

Run: `pytest -q tests/test_portable_skill_package.py -k "automatic_learning_reference or image_generation_routing or required_portable_resources"`

Expected: 全部通过。

- [ ] **Step 5: 提交本任务**

```powershell
git add -- skill-package/product-video-pipeline/SKILL.md skill-package/product-video-pipeline/references/startup-checklist.md skill-package/product-video-pipeline/references/workflow.md skill-package/product-video-pipeline/references/review-learning.md skill-package/product-video-pipeline/references/automatic-learning-rules.md skill-package/product-video-pipeline/references/image-generation-routing.md skill-package/product-video-pipeline/VERSION tests/test_portable_skill_package.py
git commit -m "docs: require verified automatic learning loop"
```

### Task 6: 全量验证与发布包

**Files:**
- Modify/Create: `release/product-video-pipeline-v1.4.0.zip`
- Test: `tests/test_portable_skill_package.py`

**Interfaces:**
- Produces: 自包含、可交给其他智能体安装的 v1.4.0 ZIP。

- [ ] **Step 1: 运行完整测试**

Run: `pytest -q tests/test_portable_skill_package.py`

Expected: 全部通过，无 warning 或 error。

- [ ] **Step 2: 运行技能包自检**

Run: `python skill-package/product-video-pipeline/scripts/self_test.py`

Expected: exit code 0。

- [ ] **Step 3: 检查技能文档和工作树格式**

```powershell
rg -n "重跑通过说明修改可能有效，但学习模式仍要用户确认" skill-package/product-video-pipeline
git diff --check
```

Expected: 第一条无匹配；第二条无输出。

- [ ] **Step 4: 生成可移植 ZIP**

使用 PowerShell `Compress-Archive` 将 `skill-package/product-video-pipeline` 目录打包为 `release/product-video-pipeline-v1.4.0.zip`。压缩包内顶层必须是 `product-video-pipeline/`，不得包含 `.env`、API Key、缓存、测试输出或产品素材。

- [ ] **Step 5: 解压到临时目录验证发布包**

创建临时目录，解压 ZIP，检查 `SKILL.md`、`VERSION`、两个新增规则文件和三个 CLI 命令，并从解压后的目录运行 `scripts/self_test.py`。

Expected: `VERSION` 为 `1.4.0`，所有文件存在，自检通过。

- [ ] **Step 6: 提交发布包**

```powershell
git add -- release/product-video-pipeline-v1.4.0.zip
git commit -m "chore: package product video pipeline v1.4.0"
```
