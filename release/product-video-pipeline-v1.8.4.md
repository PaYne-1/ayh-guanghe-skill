# product-video-pipeline 1.8.4

- 启动第一步运行 startup_gate.py prompt，完整输出三段式固定清单，等待本次用户填写确认。
- init 新增 --startup-confirmation；缺少记录、记录未确认、必填项缺失或参数不一致时，在扫描产品目录之前拒绝执行。
- 确认记录由宿主依据真实用户回复建立，不可伪造；检查不是对任意代码执行的权限隔离。
- 对话 API 配置支持中文多行原文；第三方图片和文本共用地址、密钥，模型独立，AutoDL 单独保存。显式连接冲突时拒绝写入。
- 同类别已保存的服务商、地址可复用；单价可选且不询问，缺少时标记未核实，所有连接必填项校验通过后统一保存。
- 不在技能、测试或安装包中包含真实密钥，不进行付费调用。

安装完整 product-video-pipeline-v1.8.4.zip，保留本地 data 和凭据，刷新 Hermes 技能并开启新对话。不要安装仓库根目录旧 SKILL.md 或把整个仓库复制到 skills 扫描目录。

初始化旧命令必须增加本次确认文件，见 references/startup-confirmation.md。不要为了兼容旧命令绕过校验。
